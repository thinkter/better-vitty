diff --git a/lib/helpers/text.js b/lib/helpers/text.js
index db5528ebda5f52919b539fcdee4a251e2faff43f..f3d253560fbd18900a4729502496bd124038dbd4 100644
--- a/lib/helpers/text.js
+++ b/lib/helpers/text.js
@@ -2,7 +2,15 @@ import { writeCrc } from "./crc.js";
 // https://www.w3.org/TR/png/#11tEXt
 export const textChunkName = 'tEXt';
 const NULL = 0;
-const latin1Decoder = new TextDecoder('latin1');
+const LATIN1_DECODE_CHUNK_SIZE = 8192;
+function decodeLatin1(bytes) {
+    let text = '';
+    for (let offset = 0; offset < bytes.length; offset += LATIN1_DECODE_CHUNK_SIZE) {
+        const chunk = bytes.subarray(offset, offset + LATIN1_DECODE_CHUNK_SIZE);
+        text += String.fromCharCode(...chunk);
+    }
+    return text;
+}
 function validateKeyword(keyword) {
     validateLatin1(keyword);
     if (keyword.length === 0 || keyword.length > 79) {
@@ -39,13 +47,13 @@ export function readKeyword(buffer) {
     }
     const end = buffer.offset;
     buffer.reset();
-    const keyword = latin1Decoder.decode(buffer.readBytes(end - buffer.offset - 1));
+    const keyword = decodeLatin1(buffer.readBytes(end - buffer.offset - 1));
     // NULL
     buffer.skip(1);
     validateKeyword(keyword);
     return keyword;
 }
 export function readLatin1(buffer, length) {
-    return latin1Decoder.decode(buffer.readBytes(length));
+    return decodeLatin1(buffer.readBytes(length));
 }
 //# sourceMappingURL=text.js.map
\ No newline at end of file
diff --git a/src/helpers/text.ts b/src/helpers/text.ts
index 9cf837897b390f6937388512d921b8250ed37fa9..91d861015db6fa27cc1a1aaac5c5cb148c7e5c19 100644
--- a/src/helpers/text.ts
+++ b/src/helpers/text.ts
@@ -8,7 +8,16 @@ export const textChunkName = 'tEXt';
 
 const NULL = 0;
 
-const latin1Decoder = new TextDecoder('latin1');
+const LATIN1_DECODE_CHUNK_SIZE = 8192;
+
+function decodeLatin1(bytes: Uint8Array): string {
+  let text = '';
+  for (let offset = 0; offset < bytes.length; offset += LATIN1_DECODE_CHUNK_SIZE) {
+    const chunk = bytes.subarray(offset, offset + LATIN1_DECODE_CHUNK_SIZE);
+    text += String.fromCharCode(...chunk);
+  }
+  return text;
+}
 
 function validateKeyword(keyword: string) {
   validateLatin1(keyword);
@@ -55,9 +64,7 @@ export function readKeyword(buffer: IOBuffer): string {
   }
   const end = buffer.offset;
   buffer.reset();
-  const keyword = latin1Decoder.decode(
-    buffer.readBytes(end - buffer.offset - 1),
-  );
+  const keyword = decodeLatin1(buffer.readBytes(end - buffer.offset - 1));
   // NULL
   buffer.skip(1);
 
@@ -67,5 +74,5 @@ export function readKeyword(buffer: IOBuffer): string {
 }
 
 export function readLatin1(buffer: IOBuffer, length: number): string {
-  return latin1Decoder.decode(buffer.readBytes(length));
+  return decodeLatin1(buffer.readBytes(length));
 }
